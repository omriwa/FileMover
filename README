This program made for moving files from one source to
2 destination max.

1.volume is set by the user and not by the program

2.when setting a new folders name you should always choose the destinations

3.when you want to make a regular copy of the file without changing the postfix of the file
leave the file type empty
4.filling the file type input will change all
the files postfix that were selected to the input.
5.filling the type input as- just the post fix- example:png
6.filling the target-number produce a new folders destination.
